function password(id)
{
    var check_password = document.getElementById()
    if(check_password.value == id.value)
    {
        check_password.style.backgroundColor = "green"
    }
    else
    {
        check_password.style.backgroundColor = "red" 
    }
}